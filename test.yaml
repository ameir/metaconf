aws_s3_bucket.test:
    :bucket: 'ameir-test-salam-111'
    :acl: "log-delivery-write"

aws_s3_bucket_encryption.test:
    bucket: 'ameir-test-salam-111'
    server_side_encryption_configuration:
        rules:
        - apply_server_side_encryption_by_default:
            sse_algorithm: "AES256"
